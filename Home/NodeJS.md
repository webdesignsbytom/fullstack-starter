# Node JS

## Install

### Linux

### Windows

## Update

### Linux

- `npm install -g npm@latest`
- `hash -r` rehash the shell for latest version

- `npm update`
- `npm update --save` 
- `npm update --save-dev`
- 
### Windows

- `npm install -g npm@latest`
